const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `// Error propagation (Rust-style)\nresult := doWork()\n\n// Panic/recover (Go-style)\ndefer recover()\npanic("error")`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); const parser = new Parser(tokens); const ast = parser.parse(); console.log('Body length:', ast.body.length); console.log('Errors:', parser.errors.length); ast.body.forEach((s, i) => console.log(`Statement ${i}: ${s.kind}`));
