const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `const result = condition ? <Success /> : <Error />`; console.log('Testing ternary JSX:', code); const lexer = new Lexer(code); const tokens = lexer.tokenize(); const parser = new Parser(tokens); const ast = parser.parse(); console.log('AST body length:', ast.body.length); if (ast.body.length > 0) { const stmt = ast.body[0]; console.log('First statement kind:', stmt.kind); console.log('First statement:', JSON.stringify(stmt, null, 2)); }
