const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `<Component type="text" />`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); console.log('JSX test tokens:', tokens.map(t => t.type + ':' + t.value)); const parser = new Parser(tokens); const ast = parser.parse(); console.log('Parse result:', ast.body.length > 0 ? 'JSX parsed successfully' : 'Failed');
