const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `const result = condition ? <Success /> : <Error />`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); const parser = new Parser(tokens); parser.current = 10; console.log('At token 10 (second <):'); console.log('isInJSXExpressionContext():', parser.isInJSXExpressionContext()); console.log('isJSXElement():', parser.isJSXElement());
