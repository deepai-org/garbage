const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `condition ? <Success />`; console.log('Testing partial ternary JSX:', code); const lexer = new Lexer(code); const tokens = lexer.tokenize(); const parser = new Parser(tokens); try { const expr = parser.parseExpression(); console.log('Expression parsed successfully:', expr?.kind); } catch (e) { console.log('parseExpression failed:', e.message); }
