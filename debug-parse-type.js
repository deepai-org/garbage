const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `Result<Vec<T>, Error>`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); const parser = new Parser(tokens); try { const type = parser.parseType(); console.log('Type parsed:', JSON.stringify(type, null, 2)); } catch (e) { console.log('parseType error:', e.message); }
