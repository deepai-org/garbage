const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `<input type="text" />`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); console.log('Tokens:', tokens.map(t => `${t.type}:${t.value}`)); const parser = new Parser(tokens); parser.current = 0; const isHTML = parser.isHTMLTag('input'); console.log('isHTMLTag(input):', isHTML);
