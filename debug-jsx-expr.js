const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `<input type="text" />`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); const parser = new Parser(tokens); try { parser.current = 0; const expr = parser.parseExpression(); console.log('Expression result:', expr); } catch (e) { console.log('Expression error:', e.message); }
