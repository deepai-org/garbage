const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `<input type="text" />`; console.log('Testing:', code); const lexer = new Lexer(code); const tokens = lexer.tokenize(); console.log('Tokens:', tokens.map(t => ({ type: t.type, value: t.value, virtualSemi: t.virtualSemi }))); const parser = new Parser(tokens); const ast = parser.parse(); console.log('AST body length:', ast.body.length); ast.body.forEach((item, i) => console.log(`Body[${i}]:`, item));
