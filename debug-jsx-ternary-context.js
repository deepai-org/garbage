const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `const result = condition ? <Success /> : <Error />`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); const parser = new Parser(tokens); parser.current = 5; console.log('At token 5 (first <):'); console.log('isInJSXExpressionContext():', parser.isInJSXExpressionContext()); console.log('isJSXElement():', parser.isJSXElement());
