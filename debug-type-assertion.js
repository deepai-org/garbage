const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `const x = <string>value;`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); const parser = new Parser(tokens); const ast = parser.parse(); console.log('Type assertion test:'); console.log('Body length:', ast.body.length); if (ast.body[0]) console.log('Statement:', JSON.stringify(ast.body[0], null, 2).substring(0, 500));
