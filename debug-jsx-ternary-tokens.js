const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `const result = condition ? <Success /> : <Error />`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); console.log('Tokens:'); tokens.forEach((t, i) => console.log(`${i}: ${t.type}:${t.value}`)); const parser = new Parser(tokens); try { const ast = parser.parse(); console.log('Parse succeeded, body length:', ast.body.length); } catch (e) { console.log('Parse error:', e.message); }
