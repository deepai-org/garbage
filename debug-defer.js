const { Lexer } = require('./dist/lexer'); const { Parser } = require('./dist/parser'); const code = `defer recover()`; const lexer = new Lexer(code); const tokens = lexer.tokenize(); console.log('Tokens:', tokens.map(t => t.type + ':' + t.value)); const parser = new Parser(tokens); const ast = parser.parse(); console.log('Body length:', ast.body.length); if (ast.body[0]) console.log('First item:', ast.body[0]);
